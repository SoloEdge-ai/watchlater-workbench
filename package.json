{
  "name": "watchlater-workbench",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "node --test --watch tests/*.test.js",
    "check": "node scripts/check-extension.js"
  }
}
